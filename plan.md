# 2024 年国赛 B 题建模计划

## 项目口径

- 竞赛与题目：2024 年全国大学生数学建模竞赛 B 题。
- 论文语言：中文。
- 排版环境：仓库现有 XeLaTeX 环境，入口为 `paper/main.tex`。
- 完整赛题包含 4 个问题；当前已实现问题一，并将问题二更新为联合信念状态 MDP。问题三、四尚未开始。
- 用户指定的问题一方法：以二项分布为主模型，以超几何分布作有限总体、无放回抽样校核；输入包括抽样数 `n`、总体量 `N`、标称次品率与不同信度。

## 问题一目标

在标称次品率 `p0 = 0.10` 下，为抽检结果给出三态判定：

1. 以 95% 信度拒收：证据支持次品率超过标称值；
2. 以 90% 信度接收：证据支持次品率不超过标称值；
3. 证据不足：固定样本判定不下结论，需要追加预先设计的样本或采用新的序贯方案。

程序需要给出最小可用样本量、指定样本量的接收/拒收临界值、观测结果判定、有限总体修正以及信度灵敏度结果。

## 建模路线

1. 用精确单侧二项尾概率建立主判定模型。
2. 当已知有限总体 `N` 且无放回抽样时，用超几何分布替代二项近似：
   - 拒收以可接受边界 `D0 = floor(N p0)` 为最不利原假设；
   - 接收以不可接受边界 `D1 = D0 + 1` 为最不利对立边界。
3. 搜索最小样本量时区分：首次可能接收、首次可能拒收、首次同时存在两个判定域。
4. 对给定 `n` 输出接收阈值、拒收阈值和中间不确定区，不强制把所有样本结果二分。
5. 对信度与总体量做灵敏度分析，生成 CSV、JSON 和矢量 PDF 图。

## 阶段与交付物

| 阶段 | 工作 | 交付物 |
| --- | --- | --- |
| 1. 启动 | 固定范围、目录和风险 | `plan.md`、`todo.md` |
| 2. 分析建模 | 问题拆解、假设预检、公式、算法和验证计划 | `reports/ANALYSIS_MODELING_REPORT.md` |
| 3. 编码可视化 | 实现 Q1 精确检验、Q2 信念状态 MDP、测试与逐问 Figure Pack | `code/`、`programs/`、`figures/q1/`、`figures/q2/` |
| 4. 结果记录 | 保存关键数值、Bellman 残差、灵敏度和复现命令 | `results/*`、`programs/results/*`、`reports/RESULTS_REPORT.md` |
| 5. 论文接入 | 将已确认结果写入 LaTeX，补全问题一相关章节并编译 | `paper/sections/`、`output/pdf/main.pdf` |
| 6. 论文验收 | 核对结构、数字、图表、引用、代码与逐页版式 | `reports/VERIFY_REPORT.md` |
| 7. 规范重组 | 按 CUMCM B 题逐问闭环规则拆分公共章节、回填现有结果并建立八类台账 | `paper/sections/*`、`reports/PAPER_WRITING_STATE.md` |

## 主要风险与控制

- 题面没有给出有限总体 `N`：二项结果可直接给出；超几何结果必须参数化，示例总体不能冒充题目数据。
- “尽可能少”没有指定某个备择次品率及检验功效：不能据此唯一确定传统功效样本量。本轮报告三个可验证的最小样本量，并保留不确定区。
- 固定样本临界值不能在每次新增一个样品后反复套用：这样会放大总体错误率。程序输出的是预先固定 `n` 的检验方案；若后续需要真正序贯抽样，应另设 alpha 消耗或 SPRT。
- 极端小样本可能只在“全是次品”等事件下拒收，虽然满足尾概率约束，但实用功效低；结果报告必须解释这一点。
- 浮点尾概率必须经过归一化、边界和单元测试校验；禁止只凭程序正常退出判断结果正确。

## 验收标准

- 默认参数可一条命令运行，输出含模型参数和判定阈值的 CSV/JSON。
- 二项与超几何概率分布均通过归一化和边界测试。
- 所有接收阈值满足对应左尾概率不超过 `0.10`，所有拒收阈值满足对应右尾概率不超过 `0.05`。
- 接收域与拒收域不重叠；输入越界时给出明确错误。
- 正式图按问题保存源数据、PDF/SVG 和统一样式，中文坐标与图例，无图内长标题。
- Q2 所有情形通过 Bellman 残差、信念离散精度和参数敏感性验证。
- `reports/RESULTS_REPORT.md` 中数值可由列出的命令复现。
